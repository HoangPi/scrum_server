services:
  database:
    image: postgres:16.3
    env_file:
      - ./.env.dev
    ports:
      - "4567:5432"
  server:
    build: 
      dockerfile: Dockerfile
    env_file:
      - .env
    dns:
      - 1.1.1.1
    command: ["/bin/sh"]